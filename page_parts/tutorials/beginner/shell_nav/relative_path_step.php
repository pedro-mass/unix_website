<div id="stage_content">
						<h3 id="stage_subtitle">Relative Paths</h3>

						<p id="stage_description">
							One type of path is a relative path. A relative path is the path you need to 
							take "relative" to where you are right now, to get to the directory you
							want to go to. First create a directory called documents (mkdir documents). Then execute the command below.
						</p>
						
						<p id="stage_example">
							<div id="code_example">
								Code example: 
							</div>
								<code>cd documents</code>	
						</p>
						
						<p id="stage_steps">
							Since we are already in the home directory, we can use the above command to navigate to the 
							documents directory. Next enter the cd command by itself and we will be back in the home directory. 
						</p>
</div>